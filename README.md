# Expense Manager UI

Expense-only React frontend. Transactions are loaded and saved through the `expense-manager-mt` REST API.

```powershell
npm install
npm run dev
```

The default API URL is `http://localhost:8082/api`. Copy `.env.example` to `.env` to configure another URL.
